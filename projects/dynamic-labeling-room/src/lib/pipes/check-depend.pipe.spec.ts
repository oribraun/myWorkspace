import { CheckDependPipe } from './check-depend.pipe';

describe('CheckDependPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckDependPipe();
    expect(pipe).toBeTruthy();
  });
});
